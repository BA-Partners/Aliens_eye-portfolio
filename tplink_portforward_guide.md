# TP-Link EC225-G5 Port-Forward 键盘式输入规范（最精简版）

适用页面：路由器后台 -> `高级设置(NAT转发)` -> `虚拟服务器`  
目标：WAN 端口 `175.100.46.19` 上开放 `50100/50101`，内部全部落到 `private-tron-node` 的 `50051/50052`。

## 1) 登录路由器页面（凭自己密码输入）
URL 浏览器内输入：
```
192.168.0.1
```
用户名：`admin`  
密码：请使用你自己设置的密码；这里不展示任何密码与截图，固件状态以你手动输入为准。

## 2) 键盘 Tab/Enter 输入顺序（朗加粗版）

```
Tab ->
[用户名] 输入 admin
Tab
[密码] 输入你路由器密码
Tab -> Tab -> Enter
```
若想直接进 NAT 转发页，后续高级设置路径：
```
Tab -> Tab -> Tab -> Enter  # 高级设置
Tab -> Enter                # NAT 转发
Tab -> Enter                # 虚拟服务器
Tab -> Enter                # 添加
```

## 3) 每项 Fast-Fill 键盘输入格式

按照下面顺序逐项 Tab 进入，输入后直接回车下一项。
为简化记忆，写为 `[字段]=值`，一个值一行。

### 规则 A：50100 -> 50051
```
[服务端口]=50100
[IP 地址]=127.0.0.1
[内部端口]=50051
[协议]=TCP
[状态]=勾选/输入启用
[备注/名称]=tron_50100_tcp
```

### 规则 B：50101 -> 50052
```
[服务端口]=50101
[IP 地址]=127.0.0.1
[内部端口]=50052
[协议]=TCP
[状态]=勾选/输入启用
[备注/名称]=tron_50101_tcp
```

输入后执行保存按钮：
```
Tab -> Enter   # 保存
```
失败时应出现 `保存成功`，否则白页即为键盘输入位置偏移，重新从服务端口Tab进入。

## 4) 验证结果格式（你填）
登录后把以下表格照样子输出，我可以在“无浏览器密码”前提下继续判断是否透传成功。

```
规则A 服务端口=50100, 协议=TCP, 内部IP=127.0.0.1, 内部端口=50051, 状态=已启用
规则B 服务端口=50101, 协议=TCP, 内部IP=127.0.0.1, 内部端口=50052, 状态=已启用
```

对应后续验证命令（不进入路由器页面）：
```
python3 router_port_verify.py --checks 2 --no-upnp --no-natpmp
```

当前已知矛盾：
当前 `docker-compose.yml` 仅在 `50051/50052` 上做 host 映射。
如果路由器继续把 `50100/50101` 透传过来，宿主机现在没有对应监听端口。
因此此时我给你 2 个可行推进路线：

路线 I：先在路由器把 `50100->50051, 50101->50052` 写成最终形式，我再修改 docker-compose 公差映射做干净验证。
路线 II：先把 `50100` 映射到宿主机 `50100`，但 TRON 容器内部只监听 `50051/50052`，因此需要额外兼容转发。

推荐你直接用路线 I。按上表填完后，把验证格式发我，我来做下一步映射修正。
